# bash completion for ${downstream-name}-cli                               -*- shell-script -*-

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

source $DIR/install-common.sh
complete -F _cli ${downstream-name}-cli

# ex: ts=4 sw=4 et filetype=sh
